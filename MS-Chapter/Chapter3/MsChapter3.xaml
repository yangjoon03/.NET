<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Maui_study.MsChapter3"
             xmlns:local="clr-namespace:Maui_study"
             Title="MsChapter3">
    <!--바인딩 X-->
    <!--<VerticalStackLayout Margin="10">
        <HorizontalStackLayout Spacing="20">
            <Label Text="Postal Code:" VerticalOptions="Center" />
            <Entry x:Name="PostalCode" WidthRequest="100" />
            <Button x:Name="RefreshWeatherButton" Text="Refresh" WidthRequest="200" Clicked="RefreshWeatherButton_Clicked" />
        </HorizontalStackLayout>
        <Label x:Name="Humidity" Text="Humidity: ?" />
    </VerticalStackLayout>-->

    <!--바인딩-->
    <!--<VerticalStackLayout Margin="10">
        <HorizontalStackLayout Spacing="20">
            <Label Text="Postal Code:" VerticalOptions="Center" />
            <Entry Text="{Binding Location, Mode=OneWayToSource}" WidthRequest="100" />
            <Button Text="Refresh" Command="{Binding RefreshWeather}" WidthRequest="200" />
            <SearchBar Text="{Binding Location, Mode=OneWayToSource}" SearchCommand="{Binding RefreshWeather}" />
        </HorizontalStackLayout>
        <Label Text="{Binding Humidity}" />
    </VerticalStackLayout>-->

    <!--실행할려면 추가 작업을 해야함...-->
    <!--<VerticalStackLayout Margin="10">
        <VerticalStackLayout.Resources>
            <ResourceDictionary>
                <services:WeatherService x:Key="myWeatherService" />
            </ResourceDictionary>
        </VerticalStackLayout.Resources>

        <Label Text="{Binding Path=Humidity, Source={Static Resource myWeatherService}}" />
    </VerticalStackLayout>-->

    <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center">
        <Label x:Name="TargetLabel" Text="TEXT TO ROTATE" BackgroundColor="Yellow" />
        <Slider WidthRequest="100" Maximum="360"
            Value="{Binding Rotation, Mode=OneWayToSource, Source={x:Reference TargetLabel}}" />

        <ContentPage.BindingContext>
            <local:MsChapter3Binding />
        </ContentPage.BindingContext>

        <StackLayout>
            <Label Text="{Binding Title}" />
            <Entry Text="{Binding Title, Mode=TwoWay}" />
        </StackLayout>


    </VerticalStackLayout>



</ContentPage>
